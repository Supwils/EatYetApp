<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".add_procedure"></activity>
        <activity android:name=".add_nametags" />
        <activity android:name=".add_ingred" />
        <activity android:name=".Search" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Create_Account" />
        <activity android:name=".View_Comment" />
        <activity android:name=".View_Uploaded_Recipe" />
        <activity
            android:name=".Add_Comment"
            android:label="Add Comment" />
        <activity
            android:name=".View_Recipe"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
    </application>

</manifest>